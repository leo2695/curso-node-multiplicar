# Notas:

Este es mi primer código en Node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es el producto 1 de la tabla de multiplicar[número] [requerido]
  -l, --listar   Muestra la tabla en consola si esta en true
                                                     [booleano] [defecto: false]
  -t, --limite   Número del producto 2 de la tabla          [número] [requerido]
```